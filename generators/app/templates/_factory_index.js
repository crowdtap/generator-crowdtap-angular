(function() {
  'use strict';

  var Factory;

  Factory = Factory || require('rosie').Factory;

  /* Uncoment this template to start defining your factories
   *
  Factory.define('MyFactory')
    .attr('id')
    .attr('title', 'My title')
  */

}).call(this);
